# This is the console for the Project manager.

# Import some functions from auxillary modules.
import os
# from project.pclasses import pClose
# from project.pclasses import pHomeScreen

def projects_main():
    # # The user will be presented with the HomeScreen to start.
    # project_state = pHomeScreen()
    #
    # while project_state.continue_program() == True:
    #
    #     os.system('clear')
    #
    #     project_state.header()
    #     project_state.explanation()
    #
    #     if project_state.option == "":                        # This if-else block can be used to return
    #         project_state.option = project_state.options()    # the user to the main menu of the current state.
    #     else:                                                 # Otherwise, the option will be performed.
    #         project_state = pClose()

    input("Message from project manager")

    return ""
